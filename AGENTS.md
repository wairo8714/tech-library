---
description: 高凝集・疎結合・情報隠蔽の前提原則
alwaysApply: true
---

# 設計原則: 高凝集・疎結合・情報隠蔽

このプロジェクトのコードを書くときは、以下 2 つの原則だけを物差しにすること。
原則に反する提案・コードは出してはならない。違反する場合は、その旨を明示してから代替案を提示すること。

## 原則 1. 高凝集（情報隠蔽）

> **あるクラスの内部の事情は、そのクラスの中だけで完結させる。**
> **外側は「何をしてほしいか」だけを伝えれば良い。**

呼び出し側に「どうやって実現しているか」を見せた瞬間、その実現方法を変えただけで呼び出し側まで壊れる。

```ruby
# BAD: "完了する" の実現方法（done に true をセットする）が外に漏れている
task.done = true

# GOOD: "完了せよ" とお願いするだけ。中で何をしているかは Task の責任。
task.done
```

- 状態は `attr_accessor` で外に開けず、操作はメソッドで表現する。
- 「中で何をやっているか」が呼び出し側のコードから読み取れるなら、それは漏れている。

## 原則 2. 疎結合

> **あるクラスは、他のクラスの内部について最小限のことしか知らない／使わない。**

知っている相手が増えるほど、相手の変更に巻き込まれる。

- ドメインクラス（`Task` / `TodoList`）は、永続化や表示の存在を知らない。
- 「お願い」を投げるだけで、相手の中身（インスタンス変数、内部メソッド、保存形式）には触らない。

## レイヤードアーキテクチャ

このプロジェクトでは、以下の依存方向を順守すること。

```text
Interface 層 → Usecase 層 → Domain 層
                         ↘ Infrastructure 層 → Domain 層
```

- Interface 層（`app.rb`）は Usecase 層（Service）に依存する。
- Usecase 層（Service）は Domain 層と Infrastructure 層（Repository）に依存する。
- Infrastructure 層（Repository）は Domain 層に依存する。
- Domain 層（Model）は他のどの層にも依存しない。

各層は、与えられた責務以上の機能を持たないこと。
また、他の層の内部都合を知らずに動けること。

- Interface 層は、入力の解釈、表示文言、exit code、例外のユーザ向け翻訳だけを持つ。
- Usecase 層は、ユースケースの段取り、Repository 呼び出し、Domain 操作の組み立てだけを持つ。
- Domain 層は、業務概念の状態と振る舞いだけを持つ。表示、永続化、CLI 入力形式を知らない。
- Infrastructure 層は、保存／復元の具体実装だけを持つ。CLI 表示やユースケースの段取りを知らない。

## このプロジェクトでの役割分担

| ファイル | 一言で | 持って良いもの |
|---|---|---|
| `lib/task.rb` | **1 件のタスク**。自分の状態と、それを変える振る舞い。 | id/title/done と、それらを操作するメソッド |
| `lib/todo_list.rb` | **タスクの集まり**。集合に対する追加・完了・削除。 | `@tasks` と、それに対する操作メソッド |
| `lib/error.rb` | ドメインの異常事態の名前。 | 例外クラス定義のみ |
| `lib/task_repository.rb` | **保存／復元の具体実装**。永続化形式（JSON など）の知識はここだけ。 | File I/O, JSON, 形式変換 |
| `lib/todo_list_interface.rb` | **ユースケースの段取り**。Repository から取得 → ドメインに依頼 → Repository に保存。 | リポジトリ呼び出し, ドメイン操作の組み立て |
| `todo.rb` | **CLI の窓口**。ARGV を解釈してインターフェースを呼び、結果や例外をユーザ向けに翻訳。 | ARGV パース, exit code, ユーザ向け文言 |

ドメイン (`Task` / `TodoList`) は、**永続化も表示も知らない**。
インターフェース層は、ドメインや Repository を**「使う」だけで「中を覗かない」**。

## セルフチェック（コードを書いたら必ず）

書いた／変えたコードに対して、以下 3 つを自問すること。

1. **このクラスは、外を知りすぎていないか？** （疎結合）
2. **このクラスの内側を、外がのぞき込んでいないか？** （情報隠蔽）
3. **このクラスは、複数の目的を持っていないか？** （高凝集）

特に 2 番。`obj.field = value` のような代入や、`obj` の内部構造を前提にした操作が呼び出し側にあれば、それは漏れている。

## 提案時の出力ルール

このプロジェクトに対するコード変更を提案する際は、以下を守る。

1. どのファイル／クラスに手を入れるかを最初に宣言する。
2. 上記 3 つのセルフチェックの結果を一言添える（OK ならその旨だけで可）。
3. その上でコードを示す。

## 例外

- 学習用・スパイク用と明示された一時コードはこの限りではない。
- 規模が小さく分離コストの方が大きいと判断した場合は、判断理由を明示してから簡略化すること。判断を勝手に行わない。
